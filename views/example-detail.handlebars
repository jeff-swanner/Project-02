{{> nav/nav-block}}

<div class="ui container">
  <div class="eight column">
    <div class="ui card">
      <div class="header" id="card-header"><h2>{{example.text}}</h2></div>
      <div class="content" id="card-content">
        <p>
          <strong>ID</strong>: {{example.id}}</p>
        <p>
          <strong>Name</strong>: {{example.text}}</p>
        <p>
          <strong>Description:</strong> {{example.description}}</p>
          <a href="/example">← Back To Home</a>
      </div>
    </div>
  </div>
</div>

{{> modal/login-block}}
